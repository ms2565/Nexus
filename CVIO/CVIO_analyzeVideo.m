function DF_cv = CVIO_analyzeVideo(path_model, path_video, path_output)
    % import python module
    % run inference
    mod.analyzeVideo(path, opath , path);
    % DF formatting
    DF_cv.df = [];
    DF_cv.ax.frame = [];

end