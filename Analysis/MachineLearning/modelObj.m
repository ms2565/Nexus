classdef modelObj_neuralNet < handle
    properties
    end

    methods
        function modelObj = modelObj_neuralNet(modelPath)
            % neural network handle to train and infer from a neural
            % network, etc.
        end

        function Y = infer(modelObj, X)
        end

        function train(modelObj)
        end
    end
end