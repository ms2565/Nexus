function T = nex_struct2table(s)
    % Recursively convert nested struct to flat table
    % Supports scalar and multi-row fields by broadcasting scalars
    % Prefix is used internally to build nested field names

    
  
end
